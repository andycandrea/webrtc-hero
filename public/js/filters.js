function toggleLocalFilter(klass) {
  var video = $('#local-video');
  video.toggleClass(klass);
}

function toggleRemoteFilter(klass) {
  var video = $('#remote-video');
  video.toggleClass(klass);
}
